# Dependencies for the local agent runner, installed on a user's own machine.
#
# Deliberately excludes psycopg2 (local storage is SQLite), gunicorn (waitress
# serves WSGI on Windows too) and braintrust (traces are buffered locally and
# replayed by our server, which holds the key).
#
# The runner reuses the chat app's views, so it needs the web stack the server
# uses, plus the Claude Agent SDK to run the turn.

django>=4.2,<5.0
djangorestframework>=3.14
django-cors-headers>=4.3
claude-agent-sdk>=0.1.58
anthropic>=0.94.0
httpx>=0.27.0
waitress>=3.0
