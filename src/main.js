// Main entry point - registers the custom element
import LCChatbot from './components/LCChatbot.svelte';

// The component is automatically registered as a custom element
// due to the customElement: true compiler option and the 
// <svelte:options customElement="lc-chatbot" /> in the component

export default LCChatbot;

