"""Braintrust Code Scorer (auto-generated by build.py)"""

from typing import Any

import braintrust
from pydantic import BaseModel


# =============================================================================
# Configuration
# =============================================================================

PROJECT_NAME = "On Site Agent"

NAME = "$NAME"
SLUG = "$SLUG"
DESCRIPTION = "$DESCRIPTION"


class ScorerParams(BaseModel):
    input: Any
    output: Any
    expected: Any = None
    metadata: dict[str, Any] = {}


# =============================================================================
# Handler (defined by scorer)
# =============================================================================

$HANDLER


# =============================================================================
# Registration
# =============================================================================

project = braintrust.projects.create(name=PROJECT_NAME)

project.scorers.create(
    name=NAME,
    slug=SLUG,
    description=DESCRIPTION,
    handler=handler,
    parameters=ScorerParams,
    if_exists="replace",
)
